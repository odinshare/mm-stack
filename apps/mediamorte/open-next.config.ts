// Minimal OpenNext config for Cloudflare Pages.
// Defaults are fine; we just need the file present.
export default {
  // keep the default adapter (Cloudflare Pages)
  // and default output directory ".open-next"
};
